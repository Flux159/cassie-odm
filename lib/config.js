'use strict';

module.exports = {
    cassandra: {
        options: {
                keyspace: "mykeyspace",
                hosts: [
                    "127.0.0.1:9042"
                ]
            }
    },
}
