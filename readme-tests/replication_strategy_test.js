//Test keyspace creation with replication strategy (Network Topology, etc.)
//NOTE: As of V0.0.1, it is not advisable to use this for production clusters. It would probably be best to do this through cqlsh manually when setting up cassandra.

